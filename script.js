const canvas=document.getElementById('particles'),ctx=canvas.getContext('2d');let w,h,ps=[];function resize(){w=canvas.width=innerWidth*devicePixelRatio;h=canvas.height=innerHeight*devicePixelRatio;canvas.style.width=innerWidth+'px';canvas.style.height=innerHeight+'px';ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);w=innerWidth;h=innerHeight}function init(){ps=[];for(let i=0;i<110;i++)ps.push({x:Math.random()*w,y:Math.random()*h,r:Math.random()*2.2+.3,s:Math.random()*.55+.12,a:Math.random()*.65+.15,dr:(Math.random()-.5)*.18})}function draw(){ctx.clearRect(0,0,w,h);for(const p of ps){p.y-=p.s;p.x+=p.dr;if(p.y<-5){p.y=h+5;p.x=Math.random()*w}if(p.x<0||p.x>w)p.dr*=-1;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle=`rgba(80,225,255,${p.a})`;ctx.shadowBlur=9;ctx.shadowColor='rgba(20,210,255,.7)';ctx.fill()}ctx.shadowBlur=0;requestAnimationFrame(draw)}addEventListener('resize',()=>{resize();init()});resize();init();draw();
const links=[...document.querySelectorAll('nav a')];const sections=[...document.querySelectorAll('section[id],header[id]')];const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){links.forEach(l=>l.classList.toggle('active',l.getAttribute('href')==='#'+e.target.id))}}),{rootMargin:'-40% 0px -50% 0px'});sections.forEach(s=>io.observe(s));
